You are picking up the Meridian launch-readiness programme for World Routes (21-23 October 2026),
with the Boeing demo on 13 October as the dress rehearsal. John Carter (MD, Avia Solutions) is
travelling for most of the next three weeks and will decide, not build. You build.

READ FIRST, IN THIS ORDER, before doing anything:
0. C:\AviaDev\GTM-STRATEGY-ROUTES-2026.md - THE UMBRELLA: decisions taken, the two milestones
   (demo-ready 21 Oct, order-ready 7 Nov), the integrated timeline, the pre-mortem.
1. C:\AviaDev\HANDOVER-ROUTES-19Sep2026.md  - the strategy, the calendar, the workstreams, the rules.
1b. C:\AviaDev\ROUTES-COMMERCIAL-PLAN-19Sep2026.md - sales, presentation and pack, host manual,
   pricing structure, leads/CRM/feedback, website, marketing calendar, meetings, competitors.
   This prompt runs TWO TRACKS. Track 1 (steps A-K) is engineering. Track 2 (steps L-Q) is the
   commercial plan's build items. Run them in separate chats if that is cleaner; both report
   into the same handover and master list.
2. C:\AviaDev\MASTER-TASK-LIST.md            - every other open job; do not duplicate, do not lose.
3. C:\AviaDev\HANDOVER-23Aug2026.md and BASELINE-23Aug2026.md - the machines, paths, test conventions,
   and the two known-red tests you must not chase.
4. The Egnyte estate index (/Shared/Company Data/14 Avia/AI_System/ESTATE-INDEX.md) if the Egnyte
   connector is present.

YOUR FIRST SESSION, in order, and stop for John's decision where marked:
A. Ask John for a git pull on the DevPC (C:\AviaDev) and confirm HEAD before reading code.
B. SPEED, MEASURE FIRST (handover 3.1). Give John a command block that runs one cold Optimise and
   one cold Run on SJC-TPE with timing around each stage, and a second unfamiliar pair. Do not
   propose any optimisation until his pasted timings exist. Write the profile into the handover.
C. PREAGG (3.2). Read app/preagg.py and build_preagg.py. Work out exactly what building the store
   and running the identity check involves on the workstation, and give John the blocks. The
   acceptance rule is penny-identical outputs on the pinned routes; anything else is a finding,
   not a switch.
D. PERSISTENT CACHE (3.3). Design the on-disk result cache keyed on the full run signature plus
   store vintage; tell John what it touches before writing it (calibrated_forecast, /api/forecast,
   /api/optimise, the job runners). Then build it with a test that proves a restart keeps a
   pre-warmed route.
E. REQUEST QUEUE + EMAIL + LEAD CAPTURE (4.2). Before building, put the design to John: where the
   lead store lives (workstation DuckDB vs the website's D1), which sender (Resend / Postmark /
   Microsoft Graph via the Avia tenant), what the email says, what the pack contains. STOP for his
   answer. Then build: request form in stand mode, queue view, sender, email-on-complete, failures
   visible on the queue view.
F. STAND MODE (4.2): one switch that defaults annual season and a named airline, surfaces the
   request form and the pre-warmed panel. Expert mode stays visible in the nav (John, 19 Sept);
   it is not hidden, just not opened on the stand.
G. LAPTOP BUILD PROOF (4.5): prove config.py's BUILD=laptop path actually runs the current tool
   offline with copied stores on a laptop. If it does not, say so by 1 October so a decision can
   be made; do not quietly drop it.
H. DECK (6): rebuild the 2 July methodology pptx into the 10-slide stand deck described in the
   handover, consistent with the methodology note for Nick, Avia metadata, en-GB, sources on every
   figure. Read the pptx skill first. Present it to John for one round of edits.
I. PRE-WARM (3.6): BY AIRPORT first - every attending airport warmed as origin and destination
   (catchment, competing set, origin aggregates, hub boards), so any pair a visitor names runs on
   warm parts; then the 50-route panel John chooses (4.3). Ask him for both lists; do not invent
   them. Acceptance tests: a never-run pair answers Run in under 30 seconds, and Optimise on the
   narrowed default finishes in under three minutes with its first result on screen inside 30
   seconds. Optimise IS demonstrated on the stand (John, 19 Sept): it is the USP.
K. PROGRESSIVE OPTIMISE DISPLAY (3.7): the departure curve renders as soon as it is computed and
   the sweep table fills row by row as candidates finish, so a three-minute Optimise is watched,
   not waited for. Build on the existing background-job status endpoint; do not restructure the
   sweep itself.
TRACK 2, COMMERCIAL BUILD ITEMS (from ROUTES-COMMERCIAL-PLAN-19Sep2026.md):
L. STAND DECK (plan 3.1): 10 slides, Observatory palette and imagery from the rights-managed
   library (ASSETS_DIR), accuracy wording exactly as ruled, two worked routes with real charts,
   Avia author metadata, en-GB. One round of John's edits.
M. PACK + PDF (plan 3.1-3.3): tune the HTML researched pitch for a stand conversation; add a PDF
   render (headless Chrome on the workstation), A4, Avia metadata; hero image chosen by route
   end from the library; every image carrying its rights record; the PNG-to-JPEG provenance
   loss fixed or worked around before any pack ships.
N. HOST MANUAL (plan 4): STAND-HOST-MANUAL.md with the eight sections listed; a timed demo
   script; the Plan B page; objections and answers. John edits; printed by 14 Oct.
O. LEAD STORE + FEEDBACK (plan 6): one table, written by the request form, nightly Excel export
   to Egnyte, readable on a phone; the five feedback questions built into the same form. STOP
   for John on DuckDB-vs-CRM before building.
P. WEBSITE (plan 7): once John decides launch-or-landing (by 1 Oct), build the chosen one: the
   products/QSI page as the Meridian landing with a demo/beta form, or a single public landing
   page outside Access. The form writes to the lead store.
Q. MARKETING DRAFTS (plan 8): draft each week's post and email for John's approval; never post.

J. Keep HANDOVER-ROUTES-19Sep2026.md current at the end of every session: what closed, what is
   measured, what is decided, what is waiting on John. Update MASTER-TASK-LIST.md closures.

STANDING RULES, non-negotiable:
- Git is the source of truth. Pull before editing. Commit and push after, via a DevPC block John
  runs; never run git against a mounted clone yourself.
- Command blocks: labelled DevPC / Workstation Remote / Workstation Actual, open with cd, commands
  only, one machine per block. Read John's pasted transcripts as ground truth.
- Measure before optimising. The codebase has been caught guessing four times; do not be the fifth.
- Flag rather than fill. No silent defaults, no silent fallbacks, no figure without a source in the
  same sentence. Nothing labelled "illustrative" on any client-facing surface (John, 24 August).
- The accuracy claim: calibrated leads (89% within +/-20%, 82% within +/-10% on 2,915 launches),
  blind evidence only as portfolios, never a single-route blind figure. Every slide and pack
  respects this wording exactly.
- Demo-path freeze 10 October. After that, exceptional changes only, each tested on the Boeing
  flow first and approved by John.
- Ask rather than guess on anything commercial: the panel list, pricing, the licence position,
  the email wording, the sender domain.
- Do not touch the engine's demand logic before Routes. Day-of-week allocation, fare in the QSI
  score and the new aircraft types' economics are post-Routes work already on the master list.
- Avia house style on every output: UK English, no em or en dashes, active voice, no
  consultant-generic words, Avia Solutions as document author, en-GB proofing.

Start by confirming you have read the five documents, then hand John the block for step A.

W2 stand flow: status file for the controller

Adds routes/W2-STATUS.md, rewritten by the W2 build chat each session per the
controller's instruction of 21 September. One screen: state per scope item,
evidence, next action, what is needed from John, dates, commits landed.

Version 2 carries John's rulings 14 to 17, the confirmed W1/W2 boundary, and a
sixth scope item, the stand capture front end.

Recorded from this session's code reading and John's pasted transcripts:

- Plan A closed. Meridian runs through the portal on the Huawei MateBook. The
  MateBook is not the Plan B machine: HarmonyOS host, Windows 11 Pro ARM64 in a
  StratoVirt VM. Plan B is John's core x86 laptop with a 1TB external NVMe.
- Silent fallback on the live path, and wider than the laptop. Z: is per logon
  and invisible in ssh sessions, and config._resolve_egnyte_root falls back to
  the nominal Z: path when it finds no marker folder, so a server started over
  ssh resolves the MCT master to a path that does not exist.
  connection_builder.load_mct_data then returns an empty dict without a message
  and every airport cascades to a flat 90 minutes, through route_qsi into
  route_forecast.dest_metro_share. Ruling 16's startup line answers whether the
  workstation has been running this way.
- Handover 3.4's 9x sweep narrowing assumes no airline is named. The stand flow
  names one at step 1, so stand mode saves 3x from the season alone.
- Five watchpoints on ruling 15 before the mail work starts: a cold sending
  domain against a three-day show, the Graph admin-consent lead time, two emails
  where one would do, a publicly hosted pack needing the Sabre compliance check,
  and two undated cross-workstream dependencies (W3's PDF, W6's website).

No code changed. Documentation only.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01YYBfPojqHJ5EmQipR1awfH
